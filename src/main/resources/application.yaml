spring:
  application:
    name: scispread-api
  datasource:
    url: jdbc:postgresql://localhost:5433/scispread-db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
